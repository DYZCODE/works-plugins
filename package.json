{
  "name": "works-agent-install",
  "version": "0.1.12",
  "description": "One-command installer for the WORKS public agent verifier.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "works-install": "./scripts/install.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check scripts/install.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
